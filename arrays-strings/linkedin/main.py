#! /usr/bin/pyhon3

def reverse(array):
    space_count = 2
    first_word = second_word = third_word = []

    for letter in array:
        first_word.append(array)

    return first_word

def createArray(words):
    return [l for l in words]

test = createArray('perfect makes practice')

print(reverse(test))
